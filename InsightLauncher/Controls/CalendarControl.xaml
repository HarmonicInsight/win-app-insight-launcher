<UserControl x:Class="InsightLauncher.Controls.CalendarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:InsightLauncher.ViewModels"
             xmlns:models="clr-namespace:InsightLauncher.Models">

    <Border Style="{StaticResource CardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ヘッダー -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📅" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="カレンダー"
                               Style="{StaticResource SectionHeader}"
                               Margin="8,0,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Style="{StaticResource IconButton}"
                        Command="{Binding RefreshCommand}">
                    <TextBlock Text="🔄" FontSize="12"/>
                </Button>
            </Grid>

            <!-- 連携状態 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                <Border Background="#1E3A5F"
                        CornerRadius="4"
                        Padding="8,4"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8"
                                 Fill="{Binding IsOutlookConnected, Converter={StaticResource BoolToColorConverter}, ConverterParameter=Outlook}"/>
                        <TextBlock Text="Outlook"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   Margin="6,0,0,0"/>
                    </StackPanel>
                </Border>
                <Border Background="#3D1F1F"
                        CornerRadius="4"
                        Padding="8,4">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8"
                                 Fill="{Binding IsGoogleConnected, Converter={StaticResource BoolToColorConverter}, ConverterParameter=Google}"/>
                        <TextBlock Text="Google"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="11"
                                   Margin="6,0,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 日付ナビゲーション -->
            <Grid Grid.Row="2" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Style="{StaticResource IconButton}"
                        Command="{Binding PreviousDayCommand}">
                    <TextBlock Text="◀" FontSize="10"/>
                </Button>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding SelectedDateString}"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontWeight="Medium"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="今日"
                               Foreground="{StaticResource PrimaryBrush}"
                               FontSize="11"
                               HorizontalAlignment="Center"
                               Visibility="{Binding IsToday, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>

                <Button Grid.Column="2"
                        Style="{StaticResource IconButton}"
                        Command="{Binding NextDayCommand}">
                    <TextBlock Text="▶" FontSize="10"/>
                </Button>
            </Grid>

            <!-- 予定リスト -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Events}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:CalendarEvent}">
                            <Border Background="#374151"
                                    CornerRadius="6"
                                    Padding="10"
                                    Margin="0,0,0,8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                                        <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="11">
                                            <Run Text="{Binding StartTime, StringFormat=HH:mm}"/>
                                            <Run Text=" - "/>
                                            <Run Text="{Binding EndTime, StringFormat=HH:mm}"/>
                                        </TextBlock>
                                        <Border Margin="8,0,0,0"
                                                CornerRadius="3"
                                                Padding="4,1"
                                                Background="{Binding Source, Converter={StaticResource SourceToColorConverter}}">
                                            <TextBlock Text="{Binding Source}"
                                                       Foreground="White"
                                                       FontSize="9"/>
                                        </Border>
                                    </StackPanel>

                                    <TextBlock Grid.Row="1"
                                               Text="{Binding Title}"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               FontWeight="Medium"
                                               TextTrimming="CharacterEllipsis"
                                               Margin="0,4,0,0"/>

                                    <TextBlock Grid.Row="2"
                                               Text="{Binding Location}"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="11"
                                               Visibility="{Binding Location, Converter={StaticResource NullToVisibilityConverter}}"
                                               Margin="0,2,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- フッター -->
            <Grid Grid.Row="4" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Style="{StaticResource GhostButton}"
                        Command="{Binding OpenOutlookCalendarCommand}"
                        Margin="0,0,4,0">
                    <TextBlock Foreground="#3B82F6" FontSize="11">
                        <Run Text="↗"/>
                        <Run Text=" Outlook"/>
                    </TextBlock>
                </Button>

                <Button Grid.Column="1"
                        Style="{StaticResource GhostButton}"
                        Command="{Binding OpenGoogleCalendarCommand}"
                        Margin="4,0,0,0">
                    <TextBlock Foreground="#EF4444" FontSize="11">
                        <Run Text="↗"/>
                        <Run Text=" Google"/>
                    </TextBlock>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>
