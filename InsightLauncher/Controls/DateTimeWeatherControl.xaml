<UserControl x:Class="InsightLauncher.Controls.DateTimeWeatherControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:InsightLauncher.ViewModels">

    <UserControl.DataContext>
        <vm:DateTimeWeatherViewModel/>
    </UserControl.DataContext>

    <Border Style="{StaticResource CardStyle}">
        <StackPanel>
            <!-- 日付・時刻 -->
            <TextBlock Text="{Binding CurrentDate}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="13"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CurrentTime}"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontSize="36"
                       FontWeight="Light"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>

            <!-- 天気情報 -->
            <Border BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0"
                    Margin="0,16,0,0"
                    Padding="0,16,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="☀️" FontSize="32" VerticalAlignment="Center"/>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}">
                                <Run Text="{Binding Weather.Temperature, StringFormat={}{0:F0}}"/>
                                <Run Text="°C"/>
                            </TextBlock>
                            <TextBlock Text="{Binding Weather.Description}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="13"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12">
                            <Run Text="💧"/>
                            <Run Text="{Binding Weather.Humidity, StringFormat={}{0}%}"/>
                        </TextBlock>
                        <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,4,0,0">
                            <Run Text="💨"/>
                            <Run Text="{Binding Weather.WindSpeed, StringFormat={}{0:F1}m/s}"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
