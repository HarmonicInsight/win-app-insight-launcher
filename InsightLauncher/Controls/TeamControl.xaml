<UserControl x:Class="InsightLauncher.Controls.TeamControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:InsightLauncher.Models"
             xmlns:vm="clr-namespace:InsightLauncher.ViewModels">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="👥" FontSize="18" VerticalAlignment="Center"/>
                <TextBlock Text="チーム"
                           Style="{StaticResource SectionHeader}"
                           Margin="8,0,0,0"/>
                <Border Background="{StaticResource SuccessBrush}"
                        CornerRadius="10"
                        Padding="8,2"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        Visibility="{Binding OnlineCount, Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Foreground="White" FontSize="11" FontWeight="SemiBold">
                        <Run Text="{Binding OnlineCount, Mode=OneWay}"/>
                        <Run Text=" オンライン"/>
                    </TextBlock>
                </Border>
            </StackPanel>

            <Button Grid.Column="1"
                    Style="{StaticResource IconButton}"
                    Command="{Binding RefreshCommand}">
                <TextBlock Text="🔄" FontSize="12"/>
            </Button>
        </Grid>

        <!-- 未接続状態 -->
        <Border Grid.Row="1"
                Background="{StaticResource BackgroundSecondaryBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24"
                Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="💬"
                           FontSize="48"
                           HorizontalAlignment="Center"
                           Opacity="0.5"/>
                <TextBlock Text="Teams/Slackに接続"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
                <TextBlock Text="チームメンバーのステータスを確認できます"
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="12"
                           HorizontalAlignment="Center"
                           Margin="0,4,0,0"/>
                <Button Style="{StaticResource PrimaryButton}"
                        Command="{Binding ConnectCommand}"
                        Content="接続する"
                        HorizontalAlignment="Center"
                        Margin="0,16,0,0"
                        Padding="24,8"/>
            </StackPanel>
        </Border>

        <!-- メンバーリスト -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding TeamMembers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:TeamMember}">
                        <Border Background="{StaticResource BackgroundSecondaryBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="12"
                                Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- アバター -->
                                <Border Grid.Column="0"
                                        Width="40" Height="40"
                                        CornerRadius="20"
                                        Background="{StaticResource PrimaryLightBrush}"
                                        Margin="0,0,12,0">
                                    <TextBlock Text="{Binding Name[0]}"
                                               Foreground="{StaticResource PrimaryBrush}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>

                                <!-- 名前とステータスメッセージ -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               FontWeight="Medium"
                                               FontSize="13"/>
                                    <TextBlock Text="{Binding StatusMessage}"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="11"
                                               Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <!-- ステータスインジケーター -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontSize="12">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PresenceStatus.Available}">
                                                        <Setter Property="Text" Value="🟢"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PresenceStatus.Busy}">
                                                        <Setter Property="Text" Value="🔴"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PresenceStatus.Away}">
                                                        <Setter Property="Text" Value="🟡"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PresenceStatus.DoNotDisturb}">
                                                        <Setter Property="Text" Value="⛔"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static models:PresenceStatus.Offline}">
                                                        <Setter Property="Text" Value="⚫"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
