<UserControl x:Class="InsightLauncher.Controls.SurveyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:InsightLauncher.ViewModels"
             xmlns:models="clr-namespace:InsightLauncher.Models">

    <Border Style="{StaticResource CardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ヘッダー -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📋" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="アンケート"
                               Style="{StaticResource SectionHeader}"
                               Margin="8,0,0,0"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="11"
                           VerticalAlignment="Center">
                    <Run Text="{Binding CompletedCount, Mode=OneWay}"/>
                    <Run Text="/"/>
                    <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                    <Run Text=" 回答済"/>
                </TextBlock>
            </Grid>

            <!-- アンケート一覧 -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          Visibility="{Binding SelectedSurvey, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}">
                <ItemsControl ItemsSource="{Binding PendingSurveys}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:Survey}">
                            <Button Style="{StaticResource FileItemButton}"
                                    Command="{Binding DataContext.SelectSurveyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Margin="0,0,0,8"
                                    HorizontalContentAlignment="Stretch">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding Question}"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   FontWeight="Medium"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Foreground="{StaticResource TextMutedBrush}"
                                                   FontSize="11"
                                                   Margin="0,4,0,0">
                                            <Run Text="締切:"/>
                                            <Run Text="{Binding Deadline, StringFormat=M/d}"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <TextBlock Grid.Column="1"
                                               Text="›"
                                               Foreground="{StaticResource TextMutedBrush}"
                                               FontSize="18"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 回答フォーム -->
            <Grid Grid.Row="1"
                  Visibility="{Binding SelectedSurvey, Converter={StaticResource NullToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 戻るボタン -->
                <Button Grid.Row="0"
                        Style="{StaticResource GhostButton}"
                        Command="{Binding ClearSelectionCommand}"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,12">
                    <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12">
                        <Run Text="←"/>
                        <Run Text=" 一覧に戻る"/>
                    </TextBlock>
                </Button>

                <!-- 質問 -->
                <TextBlock Grid.Row="1"
                           Text="{Binding SelectedSurvey.Question}"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           FontWeight="Medium"
                           TextWrapping="Wrap"
                           Margin="0,0,0,16"/>

                <!-- 選択肢 -->
                <ItemsControl Grid.Row="2"
                              ItemsSource="{Binding SelectedSurvey.Options}"
                              Visibility="{Binding IsSubmitted, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:SurveyOption}">
                            <Button Margin="0,0,0,8"
                                    Padding="12"
                                    HorizontalContentAlignment="Stretch"
                                    Command="{Binding DataContext.SelectOptionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding Id}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#374151"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="6"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Value="True">
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource EqualityConverter}">
                                                        <Binding Path="Id"/>
                                                        <Binding Path="DataContext.SelectedOptionId" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <Setter Property="Background" Value="#1E3A5F"/>
                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="16" Height="16"
                                             Stroke="{StaticResource BorderLightBrush}"
                                             StrokeThickness="2"/>
                                    <TextBlock Text="{Binding Label}"
                                               Margin="12,0,0,0"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 送信完了 -->
                <StackPanel Grid.Row="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="{Binding IsSubmitted, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="✓"
                               FontSize="48"
                               Foreground="{StaticResource SuccessBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="回答を送信しました"
                               Foreground="{StaticResource SuccessBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,8,0,0"/>
                </StackPanel>

                <!-- 送信ボタン -->
                <Button Grid.Row="3"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding SubmitCommand}"
                        Content="回答を送信"
                        HorizontalAlignment="Stretch"
                        Margin="0,16,0,0"
                        Visibility="{Binding IsSubmitted, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                        IsEnabled="{Binding SelectedOptionId, Converter={StaticResource NullToBoolConverter}}"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
